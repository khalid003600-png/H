# تقرير بناء WolFox 1.6.1 — iOS 15.8+ / arm64

## النتيجة

اكتمل بناء WolFox من السورس المحدث بنجاح باستخدام **iPhoneOS16.5.sdk** النظيفة وهدف نشر **iOS 15.8**. يعتمد مسار الإصدار الحالي شريحة **arm64 واحدة** بصورة صريحة؛ لا تتضمن الحزم arm64e ولا تدّعي دعماً مستقلاً لها.

| بند التحقق | النتيجة |
|---|---|
| إعداد Makefile | `TARGET := iphone:latest:15.8` و`ARCHS := arm64` |
| SDK البناء | `iPhoneOS16.5.sdk` مع روابط WebKit وTBD الرسمية السليمة |
| شريحة Mach-O | `CPU_TYPE_ARM64 (16777228)` و`CPU_SUBTYPE_ARM64_ALL (0)` |
| حجم `WolFox.dylib` | 366,448 بايت |
| التوقيع | توقيع ad-hoc ناجح بواسطة `ldid -S` |
| حزمة Rootful | 131,226 بايت، بمسار `/Library/MobileSubstrate/DynamicLibraries` |
| حزمة Rootless | 131,260 بايت، بمسار `/var/jb/Library/MobileSubstrate/DynamicLibraries` |
| ملكية ملفات الحزم | `root:root` مؤكدة |
| فلتر التطبيقات | 85 Bundle IDs محددة في `WolFoxTargetBundles.txt` |
| إعادة التحميل بعد التثبيت | `postinst` يتحقق من `sbreload` في المسارين Rootful وRootless |

## الاختبارات المنفذة

اجتازت الاختبارات المتاحة على لينكس دون إخفاق. شملت النتيجة **12** اختباراً ثابتاً لهوك الموقع، و**15** اختبار محاكاة للموقع، و**10** اختبارات ثابتة للمعرفات، و**10** اختبارات محاكاة للمعرفات، و**22** فحص توافق لملفات البناء الجديدة.

## مسار إعادة البناء

```bash
cd /home/ubuntu/wolfox_v161_ios158_265_source
make clean
make package FINALPACKAGE=1
```

يعتمد `Makefile` تلقائياً على `/home/ubuntu/theos/sdks/iPhoneOS16.5.sdk` ويثبت `WOLFOX_ARCHS=arm64` لكي لا يحاول `llvm-lipo` دمج شريحتين متطابقتين.

## حدود التحقق

يثبت هذا التقرير سلامة التجميع والتوقيع والتغليف على لينكس، والتحقق البنيوي من الحزم. يلزم الاختبار التشغيلي النهائي على جهاز iOS فعلي متوافق للتحقق من تحميل التعديل، ومسارات التثبيت، وسلوك الميزات ضمن التطبيقات المصرح بها فقط.
