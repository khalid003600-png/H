# تقرير مراجعة WolFox v1.7.5

تمت مراجعة الأرشيف المرفوع وإكمال دمج زر «فصل ارتباط التفعيل» داخل شاشة إدخال الكود.

## التحقق

يستخدم `WFLicenseClient.m` الدالة `deactivateSavedLicenseWithCompletion` ويرسل `license_code` و`device_uuid` و`access_token` و`project_key` إلى `/deactivate.php`. عند نجاح الخادم فقط تُمسح جلسة التفعيل المحلية، بينما يبقى كود التفعيل محفوظًا. عند فشل الشبكة لا تُمسح البيانات.

أضيف إلى `WFActivationViewController.m` زر واضح مع مربع تأكيد، ورسالة نجاح أو فشل، وإعادة تمكين زر التفعيل بعد الفصل. الأرشيف لا يحتوي على ملفات PHP الخاصة بالخادم؛ لذلك تمت مراجعة عقد endpoint السابق على الخادم، بينما يقتصر هذا الأرشيف على مصدر عميل iOS.

## الاختبارات والبناء

اجتازت اختبارات البناء والتوافق والهوكات والجدولة واستعادة الترخيص: 59 نجاحًا و0 فشل. اكتمل بناء Rootful وRootless باستخدام `iPhoneOS16.5.sdk`، معمارية `arm64`، Bundle ID `com.Sniper.GPS-HK`، وتوقيع ad-hoc.

لم يتم تنفيذ فصل فعلي على ترخيص إنتاجي، ولم يتم تثبيت الحزمة على جهاز iOS فعلي. يجب اختبار الزر على جهاز تجريبي مرتبط بترخيص غير مهم، مع مراقبة استجابة الخادم وسجل التطبيق.
