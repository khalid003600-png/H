# WolFox Full — تعليمات رفع إصلاح الخادم

هذه الوثيقة تربط نسخة Full 2.0.0 مع لوحة `gps.p3nd.fun` وتوضح ما يجب رفعه من حزمة لوحة الإدارة.

## إعداد الربط

- عنوان API المعتمد: `https://gps.p3nd.fun/api/v1`
- معرّف المشروع العام يقرأ من `config/integration.php`.
- لا تضع `key_secret` أو بيانات قاعدة البيانات أو أي رمز ترخيص في GitHub أو داخل التطبيق.
- يجب أن يطابق سجل المشروع في قاعدة البيانات معرّف المشروع العام، وأن يحتوي على `key_secret` صالحاً.

## الملفات التي تُرفع إلى الخادم

ارفع الملفات التالية مع الحفاظ على المسارات:

- `config/config.php`
- `config/integration.php`
- `api/v1/activate.php`
- `api/v1/verify.php`
- `api/v1/deactivate.php`
- `api/v1/health.php`

خذ نسخة احتياطية قبل الاستبدال، ولا تستبدل أبداً `config/database.local.php`.

## العقد المدعوم

تقبل نقاط التفعيل والتحقق الاسمين المتوافقين:

- `code` أو `license_code`
- `device_id` أو `device_uuid`
- `access_token` أو ترويسة Bearer / X-Access-Token

يُفضّل أن يسجل التفعيل `target_bundle_id` عند إرساله، مع إبقاء `bundle_id` للتوافق القديم. التحقق يستخدم رمز HMAC عديم الحالة؛ لا تقارن الرمز بعمود غير موجود في جدول التراخيص.

## فحص سريع

بعد الرفع افتح:

`https://gps.p3nd.fun/api/v1/health.php`

ويجب أن تعود استجابة JSON بحالة `ok` أو `healthy`، من دون كشف أسرار.

## اختبار يدوي آمن

استخدم رمزاً تجريبياً مخصصاً للاختبار فقط، ولا تحفظه في السجل أو المستودع. تحقق من أن الاستجابة الناجحة تعرض حالة الترخيص وتاريخ الانتهاء، وأن الخطأ يعرض `error_code` واضحاً. عند انقطاع الشبكة يجب أن يحتفظ التطبيق بالكود المخزن، ولا يُوقف التشغيل إلا عند انتهاء مؤكد أو إلغاء صريح من لوحة الإدارة.
