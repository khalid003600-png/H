# قائمة اختبار WolFox على جهاز فعلي

لا تُعد النسخة نهائية قبل تنفيذ هذه القائمة على جهاز Jailbreak. فحوص GitHub Actions تثبت سلامة البناء والحزم فقط، ولا تستطيع إثبات عمل الهوكات داخل تطبيق iOS حقيقي.

## مصفوفة الأجهزة

| البيئة | الحزمة | المطلوب |
|---|---|---|
| iOS 15.8 على جهاز arm64 | Rootful | تثبيت، Respring، فتح التطبيق المستهدف |
| iOS 15.8 أو أحدث | Rootless | تثبيت من `/var/jb`، Respring، فتح التطبيق |
| جهاز arm64e حديث | Rootless | التحقق من قبول شريحة arm64 وتشغيل الهوكات |
| أحدث إصدار iOS مدعوم متاح | Rootless | إعادة جميع اختبارات الميزات الأساسية |

> مسار البناء المستقر ينتج arm64 فقط. لا يُعلن عن شريحة arm64e مستقلة قبل نجاح Toolchain تدعمها واختبارها على جهاز فعلي.

## اختبارات القبول

1. التفعيل: تسجيل الدخول، استعادة الترخيص، وانتهاء الجلسة دون كشف المفتاح.
2. GPS: اختيار موقع، بدء/إيقاف التزييف، المسار، الاستعادة، المفضلة والسجل.
3. Bluetooth: إنشاء ملف صالح، تفعيله، حذف الملف النشط والتأكد من توقف التزييف.
4. المعرّفات: حفظ UUID، تفعيله، إلغاء التفعيل والتأكد من ثباته داخل التطبيق المستهدف.
5. الكاميرا الافتراضية: اختيار صورة، تشغيل/إيقاف البث، حذف الصورة وإعادة فتح التطبيق.
6. الإخفاء: إخفاء الواجهة وإظهارها باختصار الصوت والعلامة العائمة.
7. لوحة المفاتيح: نسخ، لصق، إدخال، بحث، رجوع وطي.
8. الوصول: VoiceOver، حجم نص كبير، تقليل الحركة، شاشة صغيرة واتجاه RTL.
9. الحزم: إزالة Rootful/Rootless والتأكد من عدم بقاء ملفات أو تعطل التطبيق.

## تسجيل النتيجة

سجّل طراز الجهاز، إصدار iOS، نوع Jailbreak، اسم الحزمة، نتيجة كل بند، وأرفق سجلًا منزوع الأسرار وصورة عند وجود خلل.
