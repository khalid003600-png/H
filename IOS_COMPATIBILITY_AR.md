# توافق Wolfox مع iOS 15.8–26.5

## إعداد البناء

- الحد الأدنى للتشغيل: iOS 15.8.
- الحد الأعلى المستهدف في هذه النسخة: iOS 26.5.
- يتطلب البناء النهائي `iPhoneOS16.5.sdk` أو أحدث متوافق داخل Theos.
- يرفض السكربت SDK القديم افتراضياً حتى لا تُنشر حزمة تحمل توافقاً غير متحقق.
- لا تُستخدم API واجهة تتطلب إصداراً أعلى من الحد الأدنى بلا فحص `@available`.

## اختيار الحزمة

| بيئة الجيلبريك | الملف الناتج | مسار الحقن |
|---|---|---|
| Rootful | `WolFox_v1.6.1_iOS15.8-26.5_Rootful.deb` | `/Library/MobileSubstrate/DynamicLibraries` |
| Rootless | `WolFox_v1.6.1_iOS15.8-26.5_Rootless.deb` | `/var/jb/Library/MobileSubstrate/DynamicLibraries` |

اختيار Rootful أو Rootless يعتمد على الجيلبريك نفسه، وليس رقم iOS وحده.

## المعماريات

- `arm64` هي المعمارية الوحيدة في مسار البناء المستقر الحالي.
- يرفض السكربت `arm64e` بدلاً من إنتاج حزمة غير متحققة أو دمج شرائح بأدوات غير متاحة.

## ملاحظات التوافق

- `UIWindowScene` و`connectedScenes` هما المسار الأساسي لاختيار نافذة التطبيق.
- مراقبة الصوت تستخدم إشعار النظام مع KVO وهوك UIApplication لتغطية اختلاف
  تسليم ضغطات الصوت بين إصدارات iOS.
- `PHPicker` لاختيار الصورة و`AVCaptureVideoDataOutput` لاستبدال الإطارات، مع
  Core Location وCore Bluetooth، مرتبطة ببوابة الترخيص ولا تعمل الهوكات داخل
  SpringBoard أو حزم Apple.
- توافق السورس مع إصدار النظام لا يضمن توافق كل جيلبريك؛ يجب أن توفر بيئة
  الحقن MobileSubstrate أو بديلاً متوافقاً معه وأن تسمح بحقن التطبيقات المستهدفة.
