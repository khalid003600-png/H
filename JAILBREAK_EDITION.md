# WolFox Jailbreak Edition

هذا الفرع مخصص لنسخة الجيلبريك من WolFox.

## الفرع

`feature/jailbreak-edition`

## الهدف

- بناء حزم DEB لأنظمة الجيلبريك.
- دعم Rootful و Rootless من نفس المصدر.
- استهداف iOS 14 فأحدث على مستوى الكود متى كانت واجهات النظام المستخدمة متاحة.
- إبقاء توافق الجيلبريك نفسه مرتبطاً بأداة الجيلبريك والجهاز، وليس برقم iOS فقط.
- إبقاء نسخة الجيلبريك منفصلة عن نسخة Full/Lite الأساسية.
- الاستفادة من نظام البحث الموحد الموجود في فرع `feature/unified-gps-search` كأساس للتطوير.

## نطاق التوافق المستهدف

### iOS 14
- دعم كود مستهدف قدر الإمكان للبيئات التقليدية Rootful/Rootless بحسب الأداة والجهاز.
- يجب تجنب الاعتماد غير المحمي على APIs أحدث من iOS 14.

### iOS 15 إلى iOS 17.3.1
- نطاق واسع لبيئات Rootless الحديثة، ويشمل أجهزة عديدة حسب أداة الجيلبريك المستخدمة.

### iOS 18 وما بعده
- الدعم يعتمد على الجهاز والأداة المتاحة، وقد يكون جزئياً فقط.
- لا يُعلن عن دعم عام لأي إصدار إلا بعد نجاح البناء والاختبار على بيئة جيلبريك فعلية.

## سياسة البناء

- Deployment Target المستهدف لنسخة الجيلبريك: iOS 14.0.
- استخدام checks مثل `@available(iOS 15.0, *)` عند استدعاء APIs أحدث.
- إخراج حزمتين عند الإمكان:
  - Rootful DEB
  - Rootless DEB
- إبقاء arm64 كأساس، وإضافة arm64e فقط عند توفر Toolchain وSDK متوافقين فعلياً.

## البناء

البناء القياسي:

```bash
make package
```

أو مباشرة:

```bash
MIN_IOS=14.0 WOLFOX_EDITION=Full WOLFOX_VERSION=1.8.6-Jailbreak ./build_v1_deb.sh
```

## ملاحظات

ملف `build_v1_deb.sh` الحالي يحتوي بالفعل على مسار موحد لتجهيز حزم Rootful وRootless، لكن مسار البناء الحالي في الفرع ما زال يفرض `MIN_IOS=15.0`. يجب تعديل هذا القيد وفحص كل API قبل اعتماد دعم iOS 14 فعلياً.

التوافق مع الجيلبريك لا يُساوي التوافق مع iOS فقط: قد يدعم نفس إصدار iOS بعض الأجهزة دون غيرها حسب المعالج وأداة الجيلبريك.
