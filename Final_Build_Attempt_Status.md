# حالة محاولة البناء النهائي

بعد اعتماد iOS 15.8 و`iPhoneOS16.5.sdk`، اجتازت اختبارات لينكس واختبار توافق سكربت البناء بنجاح. بدأت محاولة البناء النهائي باستخدام Clang 18، `arm64` و`arm64e`، و`ldid`، وفلاتر Bundle IDs المقيدة.

أظهر خرج المتصفح تحذيرات nullability فقط في `WolFoxProStore.h` ثم توقف قبل إظهار الخطأ الحاسم في العرض المختصر. الخطوة التالية هي قراءة السجل الكامل المحفوظ لتحديد أول خطأ فعلي، ولا تُعامل تحذيرات nullability وحدها كسبب للفشل.

تم تحديد الخطأ الفعلي لاحقاً: `iPhoneOS16.5.sdk` كان يفتقد `WebKit.framework/Headers/WebKit.h`. أضيف إطار WebKit الموجود محلياً في SDK 13.7 إلى SDK 16.5 مع رأسه وملف `WebKit.tbd` فقط، ليكون اعتماد بناء متوافقاً؛ ولم يتغير أي كود في WolFox.

بعد ذلك كشف Clang الحديث ستة تعريفات `const char` غير مهيأة في `WolFoxIntegrated.mm`. تم تهيئتها جميعاً بصفر صريح لأن استخدامها مفاتيح عناوين لـ associated objects فقط. بدأت محاولة بناء ثالثة بعد الإصلاح؛ يجب استخراج سطر النتيجة النهائي من سجلها الكامل قبل تكرار أي عملية بناء.

تمت إزالة طبقة WebKit المؤقتة واستُعيد الرابط الرسمي إلى `System/Cryptexes/OS/.../WebKit.framework`. كما أُعيدت روابط TBD غير المرقمة داخل SDK 16.5 إلى ملفاتها الرسمية المحلية: `libSystem.tbd → libSystem.B.tbd` و`libc++.tbd → libc++.1.tbd` و`libobjc.tbd → libobjc.A.tbd`. الهدف هو تمكين `ld64.lld` من إيجاد مكتبات Mach-O من SDK نفسه دون خلط إصدارات SDK.

تم فحص رؤوس Mach-O للملفين الرفيعين الناتجين: كليهما يحمل `CPU_TYPE_ARM64` مع `CPU_SUBTYPE_ARM64_ALL (0)` حتى عند طلب `arm64e`. لذلك يرفض `llvm-lipo` دمجهما لأنهما الشريحة نفسها فعلياً. أضيف `-arch arm64e` صراحةً، لكن يلزم الآن استخدام toolchain يصدر subtype arm64e فعلياً أو اعتماد شريحة arm64 واحدة بدلاً من الادعاء بوجود arm64e.

أظهر اختبار Theos Clang 10 أن رابطها الافتراضي يصل إلى مكتبات SDK 16.5 الرسمية، لكنه يتوقف عند صيغة TAPI الحديثة `!tapi-tbd` في `libSystem.tbd`. هذا عيب توافق في Toolchain القديمة، لا عيب في SDK التي اجتازت فحص الروابط. لا ينبغي نقل مكتبات من SDK 13.7؛ الحل الصحيح هو تحديث Theos/LLVM إلى Toolchain تدعم TAPI الحديثة وتصدر subtype arm64e فعلياً.
